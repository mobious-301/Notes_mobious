# ✅ Unity 资产命名简写前缀规范（最推荐版本）

## 📦 通用规则

* **全小写 + 下划线**
* 类型用 **短前缀（2~4 字母）**
* 后跟语义名称
* 不要用中文、空格、特殊符号

---

# 🧱 1. 主要类型简写前缀

| 类型                  | 简写       | 示例                    |
| ------------------- | -------- | --------------------- |
| Prefab              | **pb**   | `pb_enemy_boar`       |
| Model/FBX           | **mdl**  | `mdl_knight`          |
| Material            | **mtl**  | `mtl_wood_dark`       |
| Texture             | **tex**  | `tex_char_diff`       |
| Sprite（UI图）         | **spr**  | `spr_btn_bg`          |
| Shader              | **shd**  | `shd_unlit_outline`   |
| Animation Clip      | **anim** | `anim_knight_run`     |
| Animator Controller | **ac**   | `ac_knight`           |
| ScriptableObject    | **so**   | `so_color_map`        |
| Timeline            | **tl**   | `tl_intro`            |
| Scene               | **scn**  | `scn_mainmenu`        |
| AudioClip           | **aud**  | `aud_ui_click`        |
| VFX（特效）             | **vfx**  | `vfx_explosion_small` |
| UI Prefab           | **ui**   | `ui_panel_setting`    |
| Editor 工具           | **ed**   | `ed_svg_parser`       |

---

# 🎨 2. 材质（Material）

```
mtl_<用途>_<名称>
```

示例：

* `mtl_ui_panel`
* `mtl_fx_glow`
* `mtl_pbr_steel`

---

# 🖼️ 3. 贴图（Texture）

纹理一般细分：

* `dif`（Diffuse / Albedo）
* `nrm`（Normal）
* `msk`（Mask / ORM）
* `rgh`（Roughness）
* `spc`（Specular）
* `emi`（Emission）

格式：

```
tex_<角色/物体>_<类型>
```

示例：

* `tex_knight_dif`
* `tex_knight_nrm`
* `tex_wall_msk`

---

# 🧩 4. 预制体（Prefab）

```
pb_<类别>_<名称>
```

类别可用：enemy / npc / ui / fx / prop / weapon / item

示例：

* `pb_enemy_boar`
* `pb_ui_button`
* `pb_fx_explosion`

---

# 🗿 5. 模型（FBX / Model）

```
mdl_<名称>
```

例：

* `mdl_knight`
* `mdl_tree_oak`

如包含动画，也建议分开：

```
mdl_knight.fbx
anim_knight_run.fbx
```

---

# 🎬 6. 动画（Animation Clip）

```
anim_<角色>_<动作>
```

例：

* `anim_knight_idle`
* `anim_gate_open`

Animator Controller：

```
ac_knight
```

---

# 📜 7. ScriptableObject（你项目大量会用）

```
so_<用途>_<名称>
```

在 Wo Color 8 中例：

* `so_svg_layer`
* `so_printer_cfg`
* `so_color_match`
* `so_layer_rule_8color`

---

# 🧭 8. 场景（Scene）

```
scn_<模块>_<名称>
```

例：

* `scn_mainmenu`
* `scn_svg_editor`
* `scn_test_colorblend`

---

# 🔧 9. Editor 工具（你会用）

```
ed_<模块>_<名称>
```

例：

* `ed_svg_processor`
* `ed_color_preview`
* `ed_mesh_fix`

---

# 🚀 Wo Color 8 专用简写推荐

这是为你的项目定制的：

| 类型                  | 前缀        | 示例                     |
| ------------------- | --------- | ---------------------- |
| SVG Layer 预览 Prefab | pb_svg    | `pb_svg_layer_preview` |
| 打印颜色样例 UI           | ui_color  | `ui_color_chip`        |
| 颜色映射规则 SO           | so_colmap | `so_colmap_layer8`     |
| 打印参数                | so_prt    | `so_prt_settings`      |
| 生成的模型 FBX           | mdl_out   | `mdl_out_layered`      |
